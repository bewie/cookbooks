# The next three variables specifies where the location of the RRD
# databases, the HTML output, logs and the lock/pid files.  They all
# must be writable by the user running munin-cron.  They are all
# defaulted to the values you see here.
#
# dbdir <%= @node[:munin][:home] %>/var/db
# htmldir <%= @node[:munin][:home] %>/www/docs
# logdir <%= @node[:munin][:home] %>/log/munin
# rundir  <%= @node[:munin][:home] %>/var/run/munin
#
# Where to look for the HTML templates
# tmpldir       /home/munin/conf/templates

#max_graph_jobs 6

#contact.nagios.command /usr/bin/send_nsca nagios.host.comm -c /etc/nsca.conf


# a simple host tree
## First our "local" host.
[MuninServer;localhost]
      address 127.0.0.1
      use_node_name yes


# (Exactly one) directory to include all files from.
#
includedir <%= @node[:munin][:home] %>/conf/munin-conf.d
